@model IEnumerable<Sneaker.Models.SanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
@Html.Partial("~/Views/Shared/Partials/_banner.cshtml")

<section class="ftco-section ftco-no-pt ftco-no-pb">
    <div class="container">
        @Html.Partial("~/Views/Shared/Partials/_services.cshtml")
    </div>
</section>

<section class="ftco-section bg-light">
    <div class="container">
        <div class="row justify-content-center mb-3 pb-3">
            <div class="col-md-12 heading-section text-center ftco-animate">
                <h2 class="mb-4">Khám phá bộ sưu tập mới nhất</h2>
                @*<p>Far far away, behind the word mountains, far from the countries Vokalia and Consonantia</p>*@
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-sm-12 col-md-6 col-lg-3 ftco-animate d-flex">
                    <div class="product d-flex flex-column">
                        <a href="#" class="img-prod">
                            <img class="img-fluid" src="@Url.Content("~/Assets/Images/Products/"+item.AnhDD)" alt="@item.TenSP" width="300" height="300">
                            <div class="overlay"></div>
                        </a>
                        <div class="text py-3 pb-4 px-3">
                            <div class="d-flex">
                                <div class="cat">
                                    <span>Lifestyle</span>
                                </div>
                                <div class="rating">
                                    <p class="text-right mb-0">
                                        <a href="#"><span class="ion-ios-star-outline"></span></a>
                                        <a href="#"><span class="ion-ios-star-outline"></span></a>
                                        <a href="#"><span class="ion-ios-star-outline"></span></a>
                                        <a href="#"><span class="ion-ios-star-outline"></span></a>
                                        <a href="#"><span class="ion-ios-star-outline"></span></a>
                                    </p>
                                </div>
                            </div>
                            <h3><a href="#">@item.TenSP</a></h3>
                            <div class="pricing">
                                <p class="price"><span>@item.GiaBan</span></p>
                            </div>
                            <p class="bottom-area d-flex px-3">
                                <a href="@Url.Action("Detail","Shop", new { id = item.MaSP})" class="add-to-cart text-center py-2 mr-1"><span>Chi tiết<i class="ion-ios-add ml-1"></i></span></a>
                                <a href="@Url.Action("AddToCart", "Cart", new { @iMaSP = item.MaSP, @strURL = Request.Url.ToString() })" class="buy-now text-center py-2">Thêm<span><i class="ion-ios-cart ml-1"></i></span></a>
                            </p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>

@Html.Partial("~/Views/Shared/Partials/_feedback.cshtml")
@Html.Partial("~/Views/Shared/Partials/_followus.cshtml")

